# File Specification: https://gist.github.com/Shadows-of-Fire/88ac714a758636c57a52e32ace5474c1

# Apotheosis Balancer - Attribute Multipliers

multiplier {
    # Multiplier for generic.max_health attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.max_health"=1.0

    # Multiplier for generic.follow_range attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.follow_range"=1.0

    # Multiplier for generic.knockback_resistance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.knockback_resistance"=1.0

    # Multiplier for generic.movement_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.movement_speed"=1.0

    # Multiplier for generic.flying_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.flying_speed"=1.0

    # Multiplier for generic.attack_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.attack_damage"=1.0

    # Multiplier for generic.attack_knockback attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.attack_knockback"=1.0

    # Multiplier for generic.attack_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.attack_speed"=1.0

    # Multiplier for generic.armor attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.armor"=1.0

    # Multiplier for generic.armor_toughness attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.armor_toughness"=1.0

    # Multiplier for generic.luck attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:generic.luck"=1.0

    # Multiplier for zombie.spawn_reinforcements attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:zombie.spawn_reinforcements"=1.0

    # Multiplier for horse.jump_strength attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"minecraft:horse.jump_strength"=1.0

    # Multiplier for critical_bonus_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:critical_bonus_damage"=1.0

    # Multiplier for water_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:water_speed"=1.0

    # Multiplier for water_visibility attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:water_visibility"=1.0

    # Multiplier for lung_capacity attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:lung_capacity"=1.0

    # Multiplier for lava_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:lava_speed"=1.0

    # Multiplier for lava_visibility attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:lava_visibility"=1.0

    # Multiplier for dig_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:dig_speed"=1.0

    # Multiplier for bonus_rare_loot_rolls attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:bonus_rare_loot_rolls"=1.0

    # Multiplier for bonus_loot_count_rolls attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:bonus_loot_count_rolls"=1.0

    # Multiplier for dropped_experience attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:dropped_experience"=1.0

    # Multiplier for magic_protection attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additionalentityattributes:magic_protection"=1.0

    # Multiplier for fishing_lure attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additional_attributes:fishing_lure"=1.0

    # Multiplier for fishing_luck attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additional_attributes:fishing_luck"=1.0

    # Multiplier for looting attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additional_attributes:looting"=1.0

    # Multiplier for respiration attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additional_attributes:respiration"=1.0

    # Multiplier for harvest attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additional_attributes:harvest"=1.0

    # Multiplier for apothic_crafting attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"additional_attributes:apothic_crafting"=1.0

    # Multiplier for armor_pierce attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:armor_pierce"=1.0

    # Multiplier for armor_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:armor_shred"=1.0

    # Multiplier for arrow_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:arrow_damage"=1.0

    # Multiplier for arrow_velocity attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:arrow_velocity"=1.0

    # Multiplier for cold_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:cold_damage"=1.0

    # Multiplier for crit_chance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:crit_chance"=1.0

    # Multiplier for crit_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:crit_damage"=1.0

    # Multiplier for current_hp_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:current_hp_damage"=1.0

    # Multiplier for dodge_chance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:dodge_chance"=1.0

    # Multiplier for draw_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:draw_speed"=1.0

    # Multiplier for experience_gained attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:experience_gained"=1.0

    # Multiplier for fire_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:fire_damage"=1.0

    # Multiplier for ghost_health attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:ghost_health"=1.0

    # Multiplier for healing_received attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:healing_received"=1.0

    # Multiplier for life_steal attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:life_steal"=1.0

    # Multiplier for mining_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:mining_speed"=1.0

    # Multiplier for overheal attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:overheal"=1.0

    # Multiplier for prot_pierce attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:prot_pierce"=1.0

    # Multiplier for prot_shred attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:prot_shred"=1.0

    # Multiplier for elytra_flight attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:elytra_flight"=1.0

    # Multiplier for creative_flight attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"attributeslib:creative_flight"=1.0

    # Multiplier for fall_flying attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"caelus:fall_flying"=1.0

    # Multiplier for swim_speed attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:swim_speed"=1.0

    # Multiplier for nametag_distance attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:nametag_distance"=1.0

    # Multiplier for entity_gravity attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:entity_gravity"=1.0

    # Multiplier for block_reach attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:block_reach"=1.0

    # Multiplier for entity_reach attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:entity_reach"=1.0

    # Multiplier for step_height_addition attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"forge:step_height_addition"=1.0

    # Multiplier for ars_nouveau.perk.warding attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.warding"=1.0

    # Multiplier for ars_nouveau.perk.mana_regen attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.mana_regen"=1.0

    # Multiplier for ars_nouveau.perk.max_mana attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.max_mana"=1.0

    # Multiplier for ars_nouveau.perk.spell_damage attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.spell_damage"=1.0

    # Multiplier for ars_nouveau.perk.saturation attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.saturation"=1.0

    # Multiplier for ars_nouveau.perk.wixie attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.wixie"=1.0

    # Multiplier for ars_nouveau.perk.feather attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_nouveau.perk.feather"=1.0

    # Multiplier for ars_elemental.perk.summon_power attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"ars_nouveau:ars_elemental.perk.summon_power"=1.0

    # Multiplier for max_stamina attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"parcool:max_stamina"=1.0

    # Multiplier for stamina_recovery attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"parcool:stamina_recovery"=1.0

    # Multiplier for not_enough_glyphs.perk.mana_discount attribute.
    # This multiplier is applied ON TOP of the global Apotheosis multiplier.
    # Final value = global_multiplier * attribute_multiplier * base_value
    # Examples:
    #   1.0 = no change (uses global multiplier only)
    #   0.5 = reduce this attribute by 50%
    #   2.0 = double this attribute
    # Default: 1.0; Range: [0.0 ~ 100.0]
    S:"not_enough_glyphs:not_enough_glyphs.perk.mana_discount"=1.0
}


